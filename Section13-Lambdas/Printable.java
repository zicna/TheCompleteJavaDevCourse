public interface Printable{
    public void print();
    
    // ! if it is a functional interface it can only have one non default and non static abstract method
    // public void printWithArg(String suffix);
}